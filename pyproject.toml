[project]
name = "viassitant"
version = "0.1.0"
description = "Vibe Investment Multi-Agent Analysis Engine"
requires-python = ">=3.13"
dependencies = [
    "langgraph>=0.2",
    "langchain-openai>=0.2",
    "rich>=13.0",
    "python-dotenv>=1.0",
]

[project.scripts]
vibe = "main:main"
